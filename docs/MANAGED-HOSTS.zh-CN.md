# 远程 Linux 主机管理（恢复版开发预览）

代码根据本会话中保留的实现，从远端 `5c4a130` 重建。丢失的 `60e5aa3` / `e8bd7dd` Git 对象没有找回，本次会产生新提交，不能声称逐字节恢复原提交。

已恢复：SSH 凭据接入、主机指纹检查、自动安装 Agent/systemd、HTTPS 主动连接、持久任务和断线去重；App 内创建/安装/启停服务器、控制台、日志、模组安装/移除、内存与端口配置、配置文件编辑、世界备份/恢复和切换。

SSH 密码/私钥不进入任务历史或大厅。主机管理凭据存入系统凭据库，和玩家邀请分开。配置文件写入要求停服，保存前检查内容哈希并备份；限制 16 KB，禁止路径越界和符号链接。任务「已接收」不等于安装完成，实际结果以后台任务为准。

首版支持 x86_64 Ubuntu/Debian/Fedora + systemd，要求 root 或免密码 sudo。服务器需要能连接 SSH 和向外访问 HTTPS；管理通道上线不代表玩家游戏网络已连通。关闭 App 不会停止远程服务。

上线前仍需发布无界面服务及 checksum 附件、部署大厅 HOSTS 绑定/迁移、发布新版 App，并在真实 Linux 主机验收。大文件/整份世界上传、远程大厅邀请、自动恢复游戏进程、定时/异地备份和 Agent 自动升级/回滚仍待补齐。备份当前位于同一台服务器。

详见 [协议与限制](MANAGED-HOSTS.md)。本次恢复的测试结果单独记录在 [恢复验收](RECOVERY-VALIDATION.md)，不沿用上一轮测试结果。
